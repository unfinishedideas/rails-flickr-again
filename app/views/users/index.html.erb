<h2>All Users</h2>

<% if @users.any? %>
  <ul>
  <% @users.each do |user| %>
    <li><%= link_to user.username, user_path(user) %></li>
  <% end %>
  </ul>
  <% else %>
  <h3>No users</h3>
<% end %>
